# Fleet Commander — see CLAUDE.md

The working notes for AI assistants live in **`CLAUDE.md`** at the repo root.
They are assistant-agnostic; this pointer exists so tools that look for
`AGENTS.md` find them too.

Also worth reading before you change anything:

- **`SECURITY.md`** — threat model, residual risks, and the rules for
  security-relevant changes (this app runs commands as root on live systems).
- **`backend/CLAUDE.md`** — the Flask backend in depth.
- **`README.md`** — what the app is and how to run it.
