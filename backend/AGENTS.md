# Backend guide — see CLAUDE.md

This file used to be a byte-for-byte copy of `CLAUDE.md`, differing only in
which assistant it named. Two copies drift, so there is now one:

- **`backend/CLAUDE.md`** — the backend's developer/maintenance guide.
- **`../CLAUDE.md`** — repo-wide architecture and conventions. Read it first.
- **`../SECURITY.md`** — threat model and rules for security-relevant changes.

Everything that was here lives in `CLAUDE.md`, unchanged. Please keep it that
way rather than reintroducing a duplicate.
