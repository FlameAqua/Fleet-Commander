# Legacy: the auto-update token is gone (the repo is public, so the updater
# needs no credential). Kept ignored so a leftover file on an old dev machine
# can't be committed by accident.
electron/update-token.txt

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
release-builds

# Python backend (sidecar)
backend/.venv
backend/__pycache__
backend/**/__pycache__
backend/build
backend/dist
backend/*.spec.bak

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
